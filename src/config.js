
const config = {
    // Common configuration that doesn't change between environments
    API_ENDPOINT: '/api',
    
    // Environment specific configuration
    DOMAIN_NAME: 'https://volleyboll-dev-quiet-mountain-3664.fly.dev'
  };
  
  export default config;
  